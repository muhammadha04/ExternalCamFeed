f = find(respEsti.respAngles>200);
results = respEsti.respAngles;
results(f) = results(f)-360*ones(1,length(f));

f1 = find(expData.Angels==4);
f2 = find(expData.Angels==356);

hist(results(f1),50);
h = findobj(gca,'Type','patch');
set(h,'FaceColor','r','EdgeColor','w')
hold on;
hist(results(f2),50,'r'); hold on;

mean(results(f1))
mean(results(f2))

corr1 = (sum(results(f1)>0))/length(f1)
corr2 = (sum(results(f2)<0))/length(f2)



